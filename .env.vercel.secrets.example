# Optional only — copy to .env.vercel.secrets then: npm run env:vercel
# Contact/scheduling does not need env vars (mailto + phone + WhatsApp).

NEXT_PUBLIC_PLAUSIBLE_DOMAIN=bigfivegroup.africa
NEXT_PUBLIC_GA_MEASUREMENT_ID=
NEXT_PUBLIC_SAM_VIDEO_URL=
